import { Box, Typography, Button } from "@mui/material";
import HeroSection from "../components/HeroSection";

const Home = () => {
  return (
    <Box>
    <HeroSection/>
    </Box>
  );
};

export default Home;